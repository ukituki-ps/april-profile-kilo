# План: задача 054 — виджет `entity-types-widget`

- **Задача:** [`TASK.md`](./TASK.md)
- **Статус плана:** черновик исполнителя

## Шаги

1. Свести финальные методы провайдера с OpenAPI после 053.
2. Создать `EntityTypesWidgetCore` (state-only) и контракт провайдера (types file).
3. Реализовать `openapiEntityTypesProvider` с `AbortSignal` и корреляцией ошибок.
4. Сборка UI master–detail на `CardListColumn` и вкладки DS; редактор JSON без обходных путей.
5. Telemetry + тестовая матрица уровня profiles-widget baseline.

 [`docs/AGENT_PLAN_TEMPLATE.md`](../../docs/AGENT_PLAN_TEMPLATE.md)
