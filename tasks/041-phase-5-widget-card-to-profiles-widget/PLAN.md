# План: выделение из `widget-card` самостоятельного виджета `Profiles`

- **Задача:** [`TASK.md`](./TASK.md)
- **Дата плана:** 2026-04-28
- **Статус плана:** согласован

## Исходные допущения

- Реализация UX из задачи 040 фактически живёт в `ProfilesListWidget` и уже покрывает целевой master-detail поток.
- Для задачи 041 достаточно выделить и закрепить продуктовый контракт под именем `ProfilesWidget` без изменения backend/OpenAPI.
- Обратная совместимость важна: существующие встраивания `ProfilesListWidget` не должны ломаться.

## Порядок работ (шаги)

1. Добавить отдельный публичный компонент `ProfilesWidget` в `@april/profile-ui` как основной контракт виджета.
2. Оставить `ProfilesListWidget` как backward-compatible alias к `ProfilesWidget`.
3. Обновить публичные экспорты, demo-route в `frontend/src/App.tsx` и README пакета.
4. Актуализировать тесты: целевой сценарий должен использовать `ProfilesWidget` как основной интерфейс.
5. Обновить docs-каталог виджетов (`docs/widgets`) и человекопонятную историю в `docs-site`.
6. Прогнать проверки (`lint`, `test`, `build`) и оформить `REPORT.md`.

## Затрагиваемые области

| Область | Что меняется (кратко) |
|--------|------------------------|
| Backend (Go) | Не меняется |
| Frontend | Новый компонент `ProfilesWidget`, экспорт/alias, demo-маршрут и тесты |
| БД / Atlas | Не применяется |
| Инфра / Compose | Не применяется |
| Документация / OpenAPI | README пакета, каталог `docs/widgets`, docs-site story и overview |

## Риски и откат

- **Риск:** регрессия для текущих интеграций `ProfilesListWidget` → **Митигация:** сохранить alias и не менять поведение базовой реализации.
- **Риск:** рассинхрон документации и публичного API → **Митигация:** обновить README, docs/widgets и docs-site в том же изменении.
- При необходимости отката: revert изменений в `@april/profile-ui`, demo и документации одним PR.

## Проверка после выполнения

- Команды:
  - `cd frontend && npm run lint && npm run test && npm run build`
- Ручная проверка:
  - маршрут `profiles-widget-demo` открывает виджет `Profiles`;
  - список/карточка/edit/create/delete работают как в задаче 040;
  - существующий импорт `ProfilesListWidget` остаётся доступным.

## Примечания

- Задача решается в рамках frontend/docs, без изменений API и IAM-логики.
- Обновления плана:
  - 2026-04-28 — первичная версия.
