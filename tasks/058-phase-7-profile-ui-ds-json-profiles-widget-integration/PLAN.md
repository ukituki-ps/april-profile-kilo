# План: задача 058 — DS JSON в `profiles-widget`

## Предпосылки

- Завершена **057**: общий модуль JSON-редактора + peer `@april/ui`.

## Порядок работ

1. Подключить обёртку из 057 к экранам create и edit в `ProfilesWidgetCore`.
2. Убрать строковый state там, где он служил только сериализации JSON в textarea; оставить чёткую границу объекта ↔ API.
3. Оценить наличие схемы для `AprilJsonSchemaForm` из текущих данных провайдера; если нет — оставить только Tree/Source и зафиксировать в REPORT «Form отложен до появления схемы в API».
4. Обновить тесты и документацию.

## Примечание про RJSF

`AprilJsonSchemaForm` добавлять только при явной доступности JSON Schema для документа в UI; иначе scope 058 = Tree + Source + summary, без формы.
