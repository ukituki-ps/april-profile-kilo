# План: задача 052 — модель данных и миграции

- **Задача:** [`TASK.md`](./TASK.md)
- **Статус плана:** черновик исполнителя (обновить датами по мере согласования)

## Шаги

1. Прочитать ADR-0005 и финальную картожку виджета; составить точную DDL (таблицы, индексы, FK).
2. Спроектировать backfill из `entity_types` / `entities`; задокументировать edge case `draft`-only типов и сущности без publish.
3. Написать миграции Atlas; обновить `atlas.sum`.
4. Прогнать миграции на чистой/тестовой БД; зафиксировать SQL-проверки инвариантов (SELECT …).
5. Согласовать с задачей 053 фазу удаления legacy колонок (если двухэтапно).

См. шаблон: [`docs/AGENT_PLAN_TEMPLATE.md`](../../docs/AGENT_PLAN_TEMPLATE.md).
