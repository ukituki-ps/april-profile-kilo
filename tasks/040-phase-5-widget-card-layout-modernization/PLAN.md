# План: Task 040 — модернизация `widget-card` в master-detail layout

- **Задача:** [`TASK.md`](./TASK.md)
- **Дата плана:** 2026-04-28
- **Статус плана:** согласован

## Исходные допущения
- Текущая реализация `widget-card` де-факто находится в `ProfilesListWidget` и уже содержит list + CRUD сценарии.
- Backend API и OpenAPI достаточно для реализации без контрактных изменений.
- Переход в 25/75 делаем на уровне фронтенд-виджета и его документации/тестов.

## Порядок работ (шаги)
1. Перестроить `ProfilesListWidget` в двухколоночный layout (левая колонка списка и правая карточка выбранного профиля).
2. Добавить `Card List Column` UX: поиск, фильтр, пагинация, выбор активного профиля.
3. Перенести создание профиля в модальное окно под кнопку "+".
4. Реализовать просмотр/редактирование профиля справа с синхронизацией списка после сохранения.
5. Обновить unit/RTL тесты под новый сценарий, включая create-modal flow и ошибки API.
6. Обновить `frontend/packages/profile-ui/README.md` и docs-site story для задачи 040.
7. Заполнить `REPORT.md` с фактическими командами проверки и результатами.

## Затрагиваемые области
| Область | Что меняется (кратко) |
|--------|------------------------|
| Backend (Go) | Не меняется |
| Frontend | `ProfilesListWidget` (layout, create modal, detail panel), тесты, demo-описания |
| БД / Atlas | Не меняется |
| Инфра / Compose | Не меняется |
| Документация / OpenAPI | README виджета, docs-site story + overview, отчёт по задаче |

## Риски и откат
- **Риск:** регресс CRUD-flow после перестройки UI. → **Митигация:** обновить/расширить RTL+MSW тесты, прогнать `lint/test/build`.
- **Риск:** потеря синхронизации выбранного профиля и списка после update/create/delete. → **Митигация:** централизованно обновлять `items` и `selectedEntityId`.
- **Откат:** вернуть `ProfilesListWidget` и тесты к предыдущей реализации из истории git в ветке задачи.

## Проверка после выполнения
- Команды:
  - `cd frontend && npm run lint`
  - `cd frontend && npm run test`
  - `cd frontend && npm run build`
- Ручная проверка / smoke:
  - открыть локальный демо-маршрут `profiles-list-widget-demo`;
  - проверить сценарий select -> view/edit -> save;
  - проверить create через "+" и модальное окно;
  - проверить фильтр/поиск/пагинацию.

## Примечания
- Связанные артефакты: `tasks/040-phase-5-widget-card-layout-modernization/TASK.md`.
- Обновления плана: нет.
